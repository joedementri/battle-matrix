import './ui/theme.css';

/*
 * M0 entry point. The title screen is static markup in index.html for now;
 * main.ts exists as the Vite entry and owns the global stylesheet. Interactive
 * screens (draft, shop, deploy, battle) arrive with later milestones — see
 * PLANS/ultron-battle-matrix-protocol.md.
 */
